<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         displayDetailsOnTestsThatTriggerWarnings="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         failOnRisky="true">
    <testsuites>
        <testsuite name="libui">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <php>
        <!-- Headless GTK (Linux) prints an AT-SPI accessibility warning to stderr
             that corrupts PHPUnit's RunInSeparateProcess result serialization
             (the menu tests need a fresh libui per process). Disable the bridge. -->
        <env name="NO_AT_BRIDGE" value="1" force="true"/>
        <env name="GTK_A11Y" value="none" force="true"/>
    </php>
    <source>
        <include>
            <directory>src</directory>
        </include>
        <exclude>
            <directory>src/Generated</directory>
        </exclude>
    </source>
</phpunit>
